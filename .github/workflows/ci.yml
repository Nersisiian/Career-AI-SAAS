name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: ankane/pgvector:latest
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
<<<<<<< HEAD
=======
          # Устанавливаем зависимости для всех сервисов, которые нужны для тестов
>>>>>>> 86fc00bb503e9e87cfe9b36b1175188177bf1dc9
          pip install -r backend/services/user_service/requirements.txt
          pip install pytest pytest-cov

      - name: Run tests
        env:
          DATABASE_URL: postgresql://test_user:test_pass@localhost:5432/test_db
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: test_secret_key_for_ci
<<<<<<< HEAD
          PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/backend/services/user_service:${{ github.workspace }}/backend/shared
        run: |
=======
          PYTHONPATH: ${{ github.workspace }}/backend/services/user_service:${{ github.workspace }}/backend/shared
        run: |
          # Запускаем тесты из корневой папки tests
>>>>>>> 86fc00bb503e9e87cfe9b36b1175188177bf1dc9
          pytest tests/ -v --cov=backend/services/user_service/app --cov-report=xml

      - name: Upload coverage to Codecov (optional)
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          flags: unittests

  build-and-push:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - user_service
          - ml_service
          - llm_agent
          - scraper_service
          - analytics_service
          - frontend
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./backend/services/${{ matrix.service }}
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ai-career-${{ matrix.service }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
